{
  "test_name": "Test Post RabbitMQ Send Spike Load",
  "description": "测试RabbitMQ消息发送接口峰值冲击 - 突发流量测试",
  
  "http": {
    "base_url": "${sprival.url}",
    "timeout": 30,
    "max_retries": 3,
    "verify_ssl": false,
    "default_headers": {
      "Content-Type": "application/json"
    }
  },
  
  "scenario": {
    "type": "spike",
    "base_threads": 10,
    "spike_threads": 200,
    "spike_duration": 30,
    "spike_interval": 60,
    "cycles": 3
  },
  
  "report": {
    "formats": ["console", "json", "html"],
    "report_dir": "reports",
    "auto_save": true
  },
  
  "log": {
    "level": "WARNING",
    "log_to_file": true,
    "log_to_console": false,
    "log_dir": "logs"
  },
  
  "api": {
    "endpoint": "/rabbit/send",
    "method": "POST",
    "body_template": {
      "queue": "post.test.queue",
      "message": {
        "id": "${mock:int:1:10000}",
        "title": "${mock:string:10:200:alphanumeric}",
        "content": "${mock:string:100:1000:alphanumeric}",
        "timestamp": "${mock:timestamp}"
      }
    }
  }
}

