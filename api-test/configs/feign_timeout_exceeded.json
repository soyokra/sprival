{
  "test_name": "Test Feign Timeout Exceeded",
  "description": "测试 Feign 超时降级 - 延迟超过读取超时时间触发降级",
  
  "http": {
    "base_url": "${sprival.url}",
    "timeout": 30,
    "max_retries": 0,
    "verify_ssl": false,
    "default_headers": {
      "Content-Type": "application/json"
    }
  },
  
  "scenario": {
    "type": "constant",
    "threads": 1,
    "duration": 60
  },
  
  "report": {
    "formats": ["console", "json", "html"],
    "report_dir": "reports",
    "auto_save": true
  },
  
  "log": {
    "level": "INFO",
    "log_to_file": true,
    "log_to_console": true,
    "log_dir": "logs"
  },
  
  "api": {
    "endpoint": "/feign/timeout",
    "method": "GET",
    "params": {
      "delay": "6000"
    }
  }
}

